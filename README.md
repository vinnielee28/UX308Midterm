# Jasmine and visually tested modules

This template provides a starting point for solving simple problems and testing the solutions with jasmine and visually.

```bash
npm install
npm run dev
```

See `spec/hello_spec.js`, `src/hello.js` and `src/components/Hello.jsx`.